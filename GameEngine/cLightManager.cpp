
#include "cLightManager.h"
#include "sLight.h"
#include "GLCommon.h"
#include <string>

void cLightManager::Set_Light_Data(int shader_id)
{
	for (unsigned int i = 0; i < this->Lights.size(); ++i)
	{
		//std::string name = "theLights[" + i + "]";
		//GLint glGetUniformLocation(shader_id, );
	}
}