@author		Hamza Mian
@project	CND Midterm

Notes:
	- Make sure the project's SDK and Platform match your system's configuration
	- Project may only be built in Release config and x64 platform
	- Use the following command to build the project:
	[msbuild_path]MSbuild.exe [project_folder]GameEngine.vcxproj /p:Configuration=Release;Platform=x64 /t:midterm